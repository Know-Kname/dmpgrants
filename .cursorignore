# Build artifacts and dependencies
dist/
node_modules/
coverage/

# Large generated files
package-lock.json

# Deployment configs (not needed for coding)
Dockerfile
docker-compose.yml
.dockerignore

# Docs directory (deployment guide -- read on demand)
docs/
