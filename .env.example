DATABASE_URL=postgresql://user:password@localhost:5432/dmp_cemetery
JWT_SECRET=your-secret-key-change-this
PORT=3000
NODE_ENV=development

# CORS Configuration - Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Initial Admin User Setup (only used during database migration)
# Set these variables and run 'npm run db:migrate' to create the first admin user
# INITIAL_ADMIN_EMAIL=admin@yourdomain.com
# INITIAL_ADMIN_PASSWORD=YourSecurePassword123!
